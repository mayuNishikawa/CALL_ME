<%= form_with(model:[post, comment], local: false) do |f| %>
  <div class="comment_form">
    <%= f.text_field :comment, rows:'5', placeholder: "話しかけてみましょう!" %>
    <div class="submit_button">
      <%= f.button "ねぇ", type: "button", onclick: "submit();" %>
    </div>
  </div>
<% end %>

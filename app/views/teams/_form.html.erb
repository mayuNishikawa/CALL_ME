<%= form_with(model: team, local: true) do |f| %>
  <div class="input-group">
    <%= f.text_field :name, class: 'form-control', placeholder: "# チーム名" %>
  
    <div class="input-group-append">
      <%= f.submit "作成", class: "btn btn-primary btn-block disabled" %>
    </div>
  </div>
<% end %>
